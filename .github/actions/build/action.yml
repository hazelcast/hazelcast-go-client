name: Build

env:
  # Not possible to set this as a default
  # https://github.com/orgs/community/discussions/46670
  shell: bash

runs:
  using: composite
  steps:
    - shell: ${{ env.shell }}
      run: echo "GITHUB_REPOSITORY1 is ${GITHUB_REPOSITORY}"
    - shell: bash
      run: echo "GITHUB_REPOSITORY2 is ${GITHUB_REPOSITORY}"
